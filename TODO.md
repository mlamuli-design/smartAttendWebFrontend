# Course Management Frontend Fix TODO

**Current Status**: Backend API ✅ returns correct data structure, courses fetch successfully in console.

**Problem**: Course cards/filtering not displaying despite data being fetched.

## Tasks:

### 1. Fix handleEdit(course) mapping [Priority: High]
```
Backend course: {id, name, code, credits, semester, status(boolean), department_id}
